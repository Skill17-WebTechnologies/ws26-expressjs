# Copy to .env for local development:  cp .env.example .env
#
# .env is gitignored, so it stays on your machine. The deployed configuration
# lives in .env.prod instead.
DATABASE_URL="mysql://app:app@db:3306/app"
