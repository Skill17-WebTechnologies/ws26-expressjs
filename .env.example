# Copy to .env for local development:  cp .env.example .env
#
# .env is ignored by git and excluded from the Docker image, so it is yours
# alone. The deployed configuration lives in .env.prod instead.
DATABASE_URL="mysql://app:app@db:3306/app"
